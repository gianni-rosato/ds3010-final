# DS 3010 Final

Requires `uv`

To run:

```sh
uv run python main.py # checkpoint
uv run python map.py  # final map
```

The map view opens in a webview, accessible via `http://127.0.0.1:8050` in the
browser.
